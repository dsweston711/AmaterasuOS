tail [-n <count>] <file>

  Print the last lines of a file.

  Options:
    -n <count>    number of lines to print (default: 10)

  Examples:
    tail /sys/help/index
    tail -n 5 /sys/help/index
    tail -n1 /etc/hostname

cat <path>

  Print the contents of a file to the screen.

  Relative paths are resolved against the current working directory.
  Binary files are reported as unreadable with their size.

  Examples:
    cat /etc/hostname
    cat /sys/welcome.torii
